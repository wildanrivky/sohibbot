"""
Entry point for agent: {{ agent.name }}
Archetype: {{ agent.archetype }}
"""
import sys
import os

def main():
    # Ini adalah stub untuk eksekusi agent via Claude CLI
    # El Solver akan memanggil ini dengan context yang sudah disiapkan
    print(f"Running agent: {{ agent.name }}")
    # TODO: Implement logic to call Claude CLI with appropriate context and tools

if __name__ == "__main__":
    main()
